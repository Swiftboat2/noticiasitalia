rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas permisivas para desarrollo.
    // Permite leer y escribir en toda la base de datos.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
